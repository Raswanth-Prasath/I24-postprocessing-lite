# Data Directory

Canonical home for logistic-regression dataset NPZ files.

Expected canonical dataset names:
- `training_dataset_advanced.npz`
- `training_dataset_v2.npz`
- `training_dataset_v3.npz`
- `training_dataset_v4.npz`

Backward compatibility is currently preserved with legacy dataset paths in the
`Logistic Regression/` root.
