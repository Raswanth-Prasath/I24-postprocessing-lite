# Models Directory

Canonical model organization:
- `models/production/`: approved production artifacts
- `models/candidates/`: active experiment artifacts

Legacy model artifacts remain in `model_artifacts/` for backward compatibility.
